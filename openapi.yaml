openapi: 3.0.3
info:
  title: Swagger Petstore - OpenAPI 3.0
  version: 1.0.11
  description: "aaa"
paths:
  /greeting/{name}:
    get:
      operationId: run.post_greeting
      responses:
        '200':
          description: "vvv"
          content:
            text/plain:
              schema:
                type: string
      parameters:
        - name: name
          in: path
          required: true
          schema:
            type: string
